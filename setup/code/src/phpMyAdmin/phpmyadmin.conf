# phpMyAdmin default Apache configuration
Alias /pma /var/www/pma
<Directory /var/www/pma>
    Options SymLinksIfOwnerMatch
    DirectoryIndex index.php
    # limit libapache2-mod-php to files and directories necessary by pma
    <IfModule mod_php7.c>
        php_admin_value upload_tmp_dir /var/lib/phpmyadmin/tmp
        php_admin_value open_basedir /var/www/pma/:/var/lib/phpmyadmin/:/usr/share/php/phpseclib/
    </IfModule>
</Directory>
# Disallow web access to directories that don't need it
<Directory /var/www/pma/templates>
Require all denied
</Directory>
<Directory /var/www/pma/libraries>
Require all denied
</Directory>
<Directory /var/www/pma/setup/lib>
Require all denied
</Directory>



